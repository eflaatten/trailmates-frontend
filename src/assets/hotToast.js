import { toast } from "react-hot-toast";

const toastOptions = {
  style: {
    background: "#333",
    color: "#fff",
  },
  position: "top-center",
  duration: 4000,
};

export { toast, toastOptions };
